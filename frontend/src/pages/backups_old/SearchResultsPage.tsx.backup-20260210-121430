import React, { useState, useEffect } from "react";
import { Link } from "react-router-dom";
import { MapPin, Search, ChevronDown, Filter, Zap, Droplet, Star } from "lucide-react";

const SearchResultsPage: React.FC = () => {
  const [propertyType, setPropertyType] = useState('All Stays');
  const [isDropdownOpen, setIsDropdownOpen] = useState(false);

  // Categories for our African context
  const categories = ['All Stays', 'Hotels', 'Lodges', 'Apartments', 'Guest Houses'];

  return (
    <div className="min-h-screen bg-white">
      {/* 1. STICKY FILTER BAR - The Core Feature */}
      <div className="sticky top-0 z-30 bg-white border-b border-gray-100 shadow-sm px-6 py-4">
        <div className="max-w-7xl mx-auto flex flex-wrap items-center justify-between gap-4">
          
          {/* Dropdown Menu */}
          <div className="relative">
            <button 
              onClick={() => setIsDropdownOpen(!isDropdownOpen)}
              className="flex items-center gap-2 px-4 py-2 bg-gray-50 hover:bg-gray-100 rounded-full text-sm font-bold transition"
            >
              {propertyType} <ChevronDown size={16} />
            </button>
            
            {isDropdownOpen && (
              <div className="absolute top-12 left-0 w-48 bg-white border border-gray-100 shadow-xl rounded-2xl overflow-hidden animate-in fade-in zoom-in duration-200">
                {categories.map((cat) => (
                  <button
                    key={cat}
                    onClick={() => { setPropertyType(cat); setIsDropdownOpen(false); }}
                    className="w-full text-left px-5 py-3 hover:bg-blue-50 text-sm font-medium transition"
                  >
                    {cat}
                  </button>
                ))}
              </div>
            )}
          </div>

          {/* Quick Stats / Filter Summary */}
          <div className="hidden md:flex items-center gap-4 text-xs font-bold text-gray-400 uppercase tracking-widest">
            <span>Showing 48 Properties</span>
            <span className="w-1 h-1 bg-gray-300 rounded-full"></span>
            <span>Average K1,200/night</span>
          </div>

          <button className="flex items-center gap-2 px-5 py-2 border border-gray-200 rounded-full text-sm font-bold hover:border-blue-500 transition">
            <Filter size={16} /> Advanced Filters
          </button>
        </div>
      </div>

      {/* 2. RESULTS GRID */}
      <main className="max-w-7xl mx-auto px-6 py-8">
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
          {/* Mock Property Card */}
          {[1, 2, 3, 4, 5, 6].map((item) => (
            <div key={item} className="group cursor-pointer">
              <div className="relative aspect-[4/5] rounded-3xl overflow-hidden bg-gray-100 mb-4">
                <div className="absolute inset-0 bg-gradient-to-t from-black/40 to-transparent"></div>
                {/* Image Placeholder */}
                <div className="w-full h-full flex items-center justify-center text-gray-300">
                  <span className="text-xs uppercase tracking-tighter">Premium Visual Placeholder</span>
                </div>
                {/* Local Badges */}
                <div className="absolute top-4 left-4 flex gap-2">
                  <div className="bg-white/90 backdrop-blur px-3 py-1 rounded-full flex items-center gap-1 text-[10px] font-black text-blue-700 shadow-sm">
                    <Zap size={10} fill="currentColor" /> SOLAR POWERED
                  </div>
                </div>
                <div className="absolute top-4 right-4 p-2 bg-white/20 backdrop-blur rounded-full text-white hover:bg-white/40 transition">
                   <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l8.84-8.84 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg>
                </div>
              </div>

              <div className="space-y-1">
                <div className="flex justify-between items-start">
                  <h3 className="font-bold text-gray-900 text-lg leading-tight">Zambezi River Lodge</h3>
                  <div className="flex items-center gap-1 text-sm font-bold">
                    <Star size={14} fill="currentColor" className="text-yellow-400" /> 4.9
                  </div>
                </div>
                <p className="text-gray-500 text-sm flex items-center gap-1">
                  <MapPin size={14} /> Livingstone, Zambia
                </p>
                <div className="flex items-center gap-3 pt-2">
                  <span className="text-xl font-black text-gray-900">K1,850</span>
                  <span className="text-gray-400 text-sm font-medium">/ night</span>
                </div>
              </div>
            </div>
          ))}
        </div>
      </main>
    </div>
  );
};

export default SearchResultsPage;
